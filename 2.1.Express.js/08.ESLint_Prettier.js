// 🔴🔴 SETUP for ESLint + prettier

// ESLINT = ECMAScript + lint = linters check your code for syntax errors and highlight errors to make sure you can quickly find and fix them

//🔸SO Eslint is basically a program that constantly scans our code
// and finds potential coding errors or simply bad coding practices that
// it thinks are wrong, its very very configurable so that we can really
// fine tune it to our needs and coding habits.

// Now we also use ESLint for code formatting, but will use prettier fornow

//🔸 we need to first intall some stuff:
//🔸EXTENSIONS:
// ESLint
// Prettier

//🔸PACKAGES:
// eslint
// prettier
// eslint-config-airbnb         // this one is javscript style guide
// eslint-config-prettier       // this one disable formatting for eslint, because we want pretteir to format our code
// eslint-plugin-import         // styleguide depends on this
// eslint-plugin-jsx-a11y       // styleguide depends on this
// eslint-plugin-node           // this one will add a couple of specific elint rules only for nodejs
// eslint-plugin-prettier       // this one allows eslint to show formatting error, as we type using prettier
// eslint-plugin-react          // styleguide depends on this, airbnb need this

//🔴 now we need to create 2 config file one for .prettierrc and other for .elsintrc.json
// and add configuration in them
